{
  "name": "@linnote/contrast-util",
  "version": "0.1.0",
  "private": true,
  "description": "Shared WCAG contrast service (core.util.contrast, Desing architecture §6.1). Used by the page background picker and the core.format.font-color plugin — an explicit, documented cross-cutting dependency both declare on this package rather than on each other.",
  "type": "module",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "devDependencies": {
    "typescript": "~5.8.3",
    "vitest": "^2.1.4"
  }
}
