{
  "name": "@linnote/plugin-sync-google-drive",
  "version": "0.1.0",
  "private": true,
  "description": "Google Drive Sync plugin (core.sync.google-drive). Google Drive API v3, equivalent OAuth2 flow and keychain storage. Implements the shared SyncProvider interface (Desing architecture §16).",
  "type": "module",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@linnote/plugin-sdk": "workspace:*"
  },
  "devDependencies": {
    "typescript": "~5.8.3",
    "vite": "^7.0.4",
    "@vitejs/plugin-react": "^4.6.0",
    "@linnote/plugin-playground": "workspace:*",
    "react": "^19.1.0",
    "react-dom": "^19.1.0"
  }
}
